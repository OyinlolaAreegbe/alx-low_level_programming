#include "main.h"

/**
 * _abs - computes the absolute value of an interger
 *
 * Return: Always 0 (Success)
 */

int _abs(int)
{

		if (n < 0)
		{
			return (-n);
		}
		else						
		{
			return (n);
		}
		return (0);

}
